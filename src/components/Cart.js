import React, { Fragment } from "react";
import { useHistory } from "react-router-dom";

const Cart = () => {
  return (
    <Fragment>
      This is the cart page, theres nothing to see here... yet
    </Fragment>
  );
};

export default Cart;
